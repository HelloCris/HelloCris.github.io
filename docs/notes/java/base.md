# Java 基础入门与核心语法

## 语言总览

::: info 跨平台原理

- **核心机制**：Java 能够在不同的操作系统上运行，依赖于在操作系统上安装对应的 **Java 虚拟机**。
- **作用**：JVM 屏蔽了底层操作系统的差异，实现了“一次编写，到处运行”。

:::

::: info JRE 和 JDK

**JRE**

- **定义**：Java 程序运行时的环境。
- **组成**：包含 JVM 和需要的核心类库。
- **用途**：如果只需要运行一个已有的 Java 程序，只需安装 JRE 即可。

**JDK**

- **定义**：Java 程序开发工具包。
- **组成**：包含 JRE + 开发人员使用的工具。
- **关键工具**：编译工具 `javac.exe` 和运行工具 `java.exe`。
- **用途**：想要开发一个 Java 程序，必须安装 JDK。

:::

::: info Java 程序开发运行流程

1. 编写程序
   - 开发者编写源代码文件（`.java` 文件）。

2. 编译程序
   - 使用 `javac` 命令进行编译。
   - **产物**：得到字节码文件（`.class` 文件），这是与平台无关的中间代码。

3. 运行程序
   - 使用 `java` 命令启动 JVM 加载并执行字节码文件。

:::

## 二、基本概念

### 常量

**定义**：在程序运行中，其值不可发生改变的量。

| 常量类型       | 说明                 | 举例                        |
| :------------- | :------------------- | :-------------------------- |
| **字符串常量** | 用双引号括起来的内容 | `"HelloWorld"`, `"程序员"`  |
| **整数常量**   | 不带小数的数字       | `666`, `-88`                |
| **小数常量**   | 带小数的数字         | `13.14`, `-5.21`            |
| **字符常量**   | 用单引号括起来的内容 | `'A'`, `'0'`, `'我'`        |
| **布尔常量**   | 布尔值，表示真假     | 只有两个值：`true`, `false` |
| **空常量**     | 一个特殊的值，空值   | 值是：`null`                |

### 数据类型

Java 是强类型语言，每种数据都有明确的类型，占用不同的内存空间。

::: info 数据类型分类

- **基本数据类型**：
  - **数值型**：
    - 整数 (`byte`, `short`, `int`, `long`)
    - 浮点数 (`float`, `double`)
  - **非数值型**：
    - 字符 (`char`)
    - 布尔 (`boolean`)
- **引用数据类型**：
  - 类 (`class`)
  - 接口 (`interface`)
  - 数组 (`[]`)

:::

::: info 基本数据类型详情表

| 数据类型   | 关键字            | 内存占用 | 取值范围                                                                   |
| :--------- | :---------------- | :------- | :------------------------------------------------------------------------- |
| **整数**   | byte              | 1        | -128 ~ 127                                                                 |
|            | short             | 2        | -32768 ~ 32767                                                             |
|            | **int (默认)**    | 4        | -2的31次方到2的31次方-1                                                    |
|            | long              | 8        | -2的63次方到2的63次方-1                                                    |
| **浮点数** | float             | 4        | 负数: -3.402823E+38到-1.401298E-45; 正数: 1.401298E-45到3.402823E+38       |
|            | **double (默认)** | 8        | 负数: -1.797693E+308到-4.9000000E-324; 正数: 4.9000000E-324到1.797693E+308 |
| **字符**   | char              | 2        | 0-65535                                                                    |
| **布尔**   | boolean           | 1        | true, false                                                                |

:::

### 变量

- **概念**：内存中的一个存储区域（在程序运行过程中，其值可以发生改变）。
- **格式**：`数据类型 变量名 = 值;` （实例：`int a = 10;`）
- **注意事项**：
  - 在 Java 中，变量必须先定义后使用，仅定义不赋值的话使用会报错。
  - `Long` 型的结尾需要加 `L`。
  - 对于 `float` 类型，需要加上 `f` 后缀。

### 类型转换

- 4.1 自动类型转换
- 4.2 强制类型转换

### 运算符

- 5.1 算术运算符
  - 5.1.1 字符+操作
  - 5.1.2 字符串+操作
- 5.2 赋值运算符
- 5.3 逻辑运算符

### 基础输入与随机数

**数据输入 Scanner 类**

- **Scanner 类**：获取用户的输入。
- **引入后创建对象**：`Scanner sc = new Scanner(System.in);`
- **接收数据**（这一步就会让用户进行输入，输入完成后才对 x 赋值）：
  - `int x = sc.nextInt();`

**随机数 Random 类**

- **引入后创建对象**：`Random r = new Random();`
- **生产随机数**：
  - `int num = r.nextInt(10);`
  - **随机范围**：0 ~ 传入数值-1

## 三、数组

### 数组初始化

- 1.1 动态初始化
- 1.2 内存分配
- 1.3 静态初始化

## 四、方法

### 方法在代码中定义

### 参数的注意事项

### 带返回值的方式定义

### 注意事项

### 方法重载

### 方法参数类型问题
